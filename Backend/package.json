{
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node": "^23.11.0"
  },
  "devDependencies": {
    "@ts-jison/parser-generator": "^0.4.1-alpha.2",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "dev": "concurrently \"tsc -w --outDir dist\" \"nodemon -q dist/index.js\"",
    "start": "node dist/index.js",
    "compile": "ts -jison src/grammar.jison -o src/grammar.ts"
  }
}
